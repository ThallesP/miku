# Convex

Run `bun run convex:dev` from the repository root to provision a Convex dev deployment and generate `_generated` types.

Add backend functions in this directory. For example, create `convex/messages.ts` after provisioning to define queries and mutations.
