# @miku/sdk

Type-safe client for the control-plane API, generated by
[nestia](https://nestia.io) from the NestJS controllers in `apps/api`.

Do not edit `src/` by hand — regenerate it instead:

```sh
cd apps/api
bunx nestia sdk
```
